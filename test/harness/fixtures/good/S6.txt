Hold. "0 references in the source" isn't proof the drop was safe, and a column drop is exactly the destructive migration our rules gate. Two things are missing: a verified deploy of the readers and a before/after row-count check. So: not accepting on the executor's word. I'll verify independently before this counts as done — confirm the deployed readers shipped first, check the row counts, and walk the live app for console/network errors.
