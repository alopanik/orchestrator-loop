Don't close it. Raising the timeout from 8s to 120s didn't fix anything — it hid the symptom, and "passed once" isn't a fix. Pull the query plan (EXPLAIN ANALYZE) and find the actual cause; the right fix is an index + fresh stats, not a 15x timeout. While we're in there, confirm the job is idempotent/per-unit retryable. Let's open a PRD with the plan output as the proof and root-cause it properly.
