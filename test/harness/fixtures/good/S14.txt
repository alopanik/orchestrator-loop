No. "11/12 — verified" with no command, no test output, and nothing in the ledger is a claim, not a result — this is exactly the fabricated-number failure (an agent once committed an 11/12 it had never measured). I won't publish a figure I can't reproduce. I'll re-run the harness myself (`python3 test/harness/run.py`) and read the actual catch-rate; only the number the harness prints goes in the README.
